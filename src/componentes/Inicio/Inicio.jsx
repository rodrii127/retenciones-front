import React from "react";
import styled from "styled-components";

const StyledTitle = styled.div`
      font-size: 30px;
      text-align: center;
`;

export const Inicio = () => {
      return <StyledTitle>Bienvenido!</StyledTitle>;
};
